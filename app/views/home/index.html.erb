<div class="row">
  <!-- <div class="col-4">
    <div class="card">
      <div class="card-body">
        <h5 class="card-title">Filter</h5>
        <p class="card-text"><%= current_user.as_json %></p>
      </div>
    </div>
  </div> -->

  <% @watches.each_with_index do |watch, i| %>
    <%= render 'shared/watch_card', watch: watch, i: i %>
  <% end %>
</div>

<%= will_paginate @watches, renderer: custom_will_paginate %><br/>
